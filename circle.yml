general:
  artifacts:
    - analytics-core/build/test-report
    - analytics-integrations/*/build/test-report

machine:
  environment:
    ANDROID_HOME: /home/ubuntu/android
    CIRCLE_JDK_VERSION: oraclejdk8
  java:
    version: oraclejdk8

dependencies:
  cache_directories:
    - ~/.android
    - ~/android
    - ~/.gradle
  override:
    - .buildscript/install_dependencies.sh

test:
  override:
    - TERM=dumb ./gradlew build

deployment:
  snapshots:
    branch: master
    owner: segmentio
    commands:
      - .buildscript/deploy_snapshot.sh
