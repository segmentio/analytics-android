machine:
  environment:
    ANDROID_HOME: /home/ubuntu/android
  java:
    version: oraclejdk8
dependencies:
  cache_directories:
    - ~/.android
    - ~/android
    - ~/.gradle
  override:
    - .buildscript/install-dependencies.sh
test:
  override:
    - TERM=dumb ./gradlew build
