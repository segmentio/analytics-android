apply from: rootProject.file('gradle/integration.gradle')

dependencies {
  compile 'com.followapps.android:sdk:3.1.0@aar'
}
repositories {
  maven {
    url 'http://nexus.follow-apps.com/nexus/content/repositories/releases/'
  }
}
